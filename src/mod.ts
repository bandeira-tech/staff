/**
 * @module
 * @bandeira-tech/staff — URI convention + (future) MCP wiring for the
 * six staff primitives: traits, roles, plays, teams, staff, sessions.
 * MVP exports the protocol surface.
 */
export * from "./protocol.ts";
